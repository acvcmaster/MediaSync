import { NoExtensionPipe } from './no-extension.pipe';

describe('NoExtensionPipe', () => {
  it('create an instance', () => {
    const pipe = new NoExtensionPipe();
    expect(pipe).toBeTruthy();
  });
});
